<!DOCTYPE html>
  <html>
  <head>
    <title>Hire Me App</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
    <body>
      <% if current_page?(controller: 'welcome', action: 'index') %>     
        <div class="container">
          <%= yield %>
        <div>
      <% else %>
        <%= render "shared/navbar", :companies => @companies %>   
        <div class="container">
          <%= yield %>
        <div>     
      <% end %>   
    </body>
    <%#  <%# Solves the emotion-serialize.browser.esm.js? [sm]:47 Uncaught ReferenceError: process is not defined
    at emotion-serialize.browser.esm.js? [sm]:47:5 error  %> 
    <script>
      const process = {}
      process.env = {}
      process.env.NODE_ENV = "<%= Rails.env %>"
    </script>
  </html>
